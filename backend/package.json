{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": ".venv/bin/python -m uvicorn app.main:app --reload --app-dir .",
    "build": ".venv/bin/python -m compileall -q app",
    "check": ".venv/bin/python -m compileall -q app",
    "start": ".venv/bin/python -m uvicorn app.main:app --app-dir ."
  }
}